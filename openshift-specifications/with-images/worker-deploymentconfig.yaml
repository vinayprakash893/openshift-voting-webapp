kind: DeploymentConfig
apiVersion: apps.openshift.io/v1
metadata:
  name: worker
  labels:
    app: voting-app
spec:
  triggers:
  - type: ConfigChange
  replicas: 1
  selector:
    app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      #securityContext:
        #runAsUser: 1001
      containers:
        - name: worker
          image: quay.apps.lz-np2.ent-ocp4-useast1.aws.internal.das/an222685ad/worker:latest
